class Task:
    def __init__(self, t_name, t_desc, t_status, t_priority, t_notes, t_bm, t_own_id, t_cr_id, crt_on, mod_on):

        self.t_name = t_name
        self.t_desc = t_desc
        self.t_status = t_status
        self.t_priority = t_priority
        self.t_notes = t_notes
        self.t_bm = t_bm
        self.t_own_id = t_own_id
        self.t_cr_id = t_cr_id
        self.crt_on = crt_on
        self.mod_on = mod_on